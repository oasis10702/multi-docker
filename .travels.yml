sudo: required
services:
  - docker

brefore_install:
  - docker build -t hiro/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run hiro/react-test npm test -- --coverage

after_success:
  - docker build -t hiro/multi-client ./client
  - docker build -t hiro/multi-nginx ./nginx
  - docker build -t hiro/multi-server ./server
  - docker build -t hiro/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push hiro/multi-client
  - docker push hiro/multi-nginx
  - docker push hiro/multi-server
  - docker push hiro/multi-worker
